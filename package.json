{
  "name": "selenium-js-docker",
  "version": "1.0.0",
  "description": "Selenium test in JS using Docker and Jenkins",
  "scripts": {
    "test": "wait-on tcp:selenium-hub:4444 && mocha tests/**/*.test.js"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "selenium-webdriver": "^4.34.0"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^10.8.2",
    "wait-on": "^8.0.4"
  }
}
